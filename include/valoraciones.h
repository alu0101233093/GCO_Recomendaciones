#include <vector>
#include <iostream>
#include <stdio.h>

typedef std::vector<std::vector<int>> Matriz;

class Valoraciones {
    private:
        Matriz CI;
    public:
        void set_person(std::string, int);

}